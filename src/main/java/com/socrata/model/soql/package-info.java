/**
 * The model objects (nouns) used for defining SoQL operations.  These objects are not strictly
 * required since there are APIs that take a SoQL string, but can be useful when trying to create
 * code that generates SoQL.
 **/
package com.socrata.model.soql;